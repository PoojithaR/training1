1. Create a class called Accounts which has data members like Account no, Customer name, Account type, Transaction type (d/w), Amount, balance
D->Deposit
W->Withdrawal
- If transaction type is deposit call the Method credit(int amount) and update balance in this method.
- If transaction type is withdraw call the method debit(int amt) and update balance
- With one constructor intialize the data members
- Pass the other information like Acount no,name,acc type through  another constructor
- call the show data method to display the values.

2. Create a class called student which has data members like rollno, name, class, Semester, branch,and
int [] marks=new int marks [5](marks of 5 subjects )
Write a method called displayresult, which should calculate the average marks
- If marks of any one subject is less than 35 print result as failed
- If marks of all subject is >35 but average is < 50 then also print result as failed
- If avg > 50 then print result as passed.
- Pass the details of student like rollno, name, class, Semester, branch in constructor
- Write a Display Data method to display all values.

3. Create a structure called Books with the following members:
	Title
	Author
	Subject
	BookId
write methods called
 GetBookDetails()
 ShowBookDetails()


Invoke all the above methods 